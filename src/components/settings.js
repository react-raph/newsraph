import React from 'react';
import {Link} from "react-router-dom";

const Settings = (props) => (
    <div>
        <h2>Settings</h2>
        <Link to="/">Return</Link>
    </div>
);

export default Settings;